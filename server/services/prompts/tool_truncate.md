You are a content condensation expert. Take the following raw tool message, which contains website content (may include HTML, navigation, ads, and repeated boilerplate). Do the following:
1. Strip all non‑content elements (e.g., navigation bars, footers, ads, cookie notices, and irrelevant sidebars) and extract only the main article or core block of text.
2. Condense the extracted text down to a maximum of {max_length} while strictly preserving:
   - All key facts, figures, names, dates, and statistics.
   - The central argument, conclusion, or take‑away message.
   - Any actionable instructions or steps (if present).
3. Use concise language, but keep original technical terms and important wording exactly as they appeared. Combine short paragraphs and omit redundant examples, digressions, and overly detailed background explanations unless they are crucial to the core message.
4. Remove all images and images related contents.
5. Output only the cleaned, truncated content — no preamble, no commentary, no markdown formatting beyond plain paragraphs.
Tool message:
{observation}